# Configuration TLS globale
# Désactive les cipher suites faibles et configure TLS 1.2/1.3 uniquement

tls:
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: true
      cipherSuites:
        - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
        - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
        - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
        - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
        - TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256
        - TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256

  # Certificat par défaut pour les domaines non configurés
  # Evite d'exposer le certificat snake-oil de Traefik
  stores:
    default:
      defaultGeneratedCert:
        resolver: letsencrypt
        domain:
          main: "azteas.com"
          sans:
            - "*.azteas.com"
