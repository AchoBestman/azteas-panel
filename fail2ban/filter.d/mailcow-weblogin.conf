# Logins échoués sur Mailcow via Traefik (access log JSON)
# POST / avec status 200 sur mail.azteas.com = échec

[Definition]
failregex = .*"ClientHost":"<HOST>".*"RequestAddr":"mail\.azteas\.com".*"RequestMethod":"POST".*"RequestPath":"\/".*"DownstreamStatus":200.*
ignoreregex =
