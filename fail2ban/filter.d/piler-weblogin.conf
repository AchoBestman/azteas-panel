# Logins échoués sur Piler via Traefik (access log JSON)
# POST /login.php avec status 200 = échec (302 = succès)

[Definition]
failregex = .*"ClientHost":"<HOST>".*"RequestMethod":"POST".*"RequestPath":"\/login\.php".*"DownstreamStatus":200.*
ignoreregex =
